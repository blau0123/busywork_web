const initState = {
    todoList:[
        {id: '1', task:'get lit'},
        {id: '2', task:'get lit again lol'},
    ]
}

const todosReducer = (state = initState, action) => {
    return state;
}

export default todosReducer;