const initState = {
    noteList: [
        {id: '1', title: "first", body: "note lol"}
    ]
}

const notesReducer = (state = initState, action) => {
    return state;
}

export default notesReducer;